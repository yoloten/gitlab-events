import io from "socket.io-client"

//const socket = io('http://83.220.170.135')

class Receiver {
    init (dispatch, type) {
        // socket.on('result', (data) => {
        //     dispatch({
        //         type   : type,
        //         payload: data
        //     });
        // });
    }
}

export default new Receiver()